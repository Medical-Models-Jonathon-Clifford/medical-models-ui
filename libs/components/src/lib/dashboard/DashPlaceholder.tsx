import { Box, BoxProps } from '@mui/material';

export const DashPlaceholder = (props: BoxProps) => (
  <Box
    sx={{
      width: '50%',
      padding: '8px',
      height: '300px',
    }}
  ></Box>
);
